spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/wirtualny_dom_kultury?serverTimezone=UTC
  jpa:
    hibernate:
      ddl-auto: create
  profiles:
    active: local
